# Never commit the real .env. Values here are placeholders only.
# Replace both image placeholders with immutable sha256 digests after candidate verification.
MESH_COS_IMAGE=mesh-cos-mcp@sha256:REPLACE_WITH_VERIFIED_DIGEST
TUNNEL_IMAGE=ghcr.io/openai/tunnel-client@sha256:REPLACE_WITH_V0_0_12_VERIFIED_DIGEST
QNAP_MESH_ROOT=/share/REPLACE_WITH_VERIFIED_PATH/mesh-cos
QNAP_TUNNEL_API_KEY_FILE=/share/REPLACE_WITH_VERIFIED_PATH/mesh-cos-secrets/openai-tunnel-runtime-key
MESH_UID=REPLACE_WITH_VERIFIED_UID
MESH_GID=REPLACE_WITH_VERIFIED_GID
MESH_CPU_LIMIT=1.0
MESH_MEMORY_LIMIT=1g
MESH_MEMORY_RESERVATION=512m
MESH_PIDS_LIMIT=128
MESH_LOG_MAX_SIZE=10m
MESH_LOG_MAX_FILES=5
MESH_COS_BRIDGE_TIMEOUT_MS=30000
MESH_COS_MAX_BRIDGE_QUEUE=32
CONTROL_PLANE_TUNNEL_ID=tunnel_REPLACE_WITH_32_HEX_CHARACTERS
