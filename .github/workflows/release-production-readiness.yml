name: release-production-readiness

on:
  push:
    branches: [main]
    paths:
      - RELEASE.md

permissions:
  contents: write

jobs:
  release:
    if: github.repository == 'mkleinbe/mesh-ai-agent-cos-universe'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Create v1.1.0 Local ChatGPT MCP release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: v1.1.0
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists; no action."
          else
            gh release create "$TAG" \
              --target "$GITHUB_SHA" \
              --title "v1.1.0 Local ChatGPT MCP" \
              --notes-file RELEASE.md
          fi
