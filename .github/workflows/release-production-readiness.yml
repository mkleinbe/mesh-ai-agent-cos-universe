name: release-production-readiness

on:
  push:
    branches: [main]
    paths:
      - RELEASE.md
      - CHANGELOG.md
      - deployment/qnap/**
      - docs/qnap-**
      - docs/chatgpt-**
      - docs/release-**
      - specs/qnap-**
      - Dockerfile
      - scripts/build-qnap-release-bundle.sh
      - .github/workflows/release-production-readiness.yml

permissions:
  contents: write

jobs:
  release:
    if: github.repository == 'mkleinbe/mesh-ai-agent-cos-universe'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build v4.1.9 QNAP deployment bundle
        run: bash scripts/build-qnap-release-bundle.sh 4.1.9
      - uses: actions/upload-artifact@v6
        with:
          name: mesh-cos-mcp-qnap-v4.1.9
          path: |
            dist/mesh-cos-mcp-qnap-v4.1.9.zip
            dist/mesh-cos-mcp-qnap-v4.1.9.zip.sha256
      - name: Create or update v4.1.9 release and semantic tag
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: v4.1.9
          ASSET: dist/mesh-cos-mcp-qnap-v4.1.9.zip
        run: |
          set -euo pipefail
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$ASSET" "$ASSET.sha256" --clobber
          else
            gh release create "$TAG" \
              --target "$GITHUB_SHA" \
              --title "v4.1.9 Documentation and Release Closeout" \
              --notes-file RELEASE.md \
              "$ASSET" "$ASSET.sha256"
          fi
