name: v4.1.18 historical release verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v4.1.18 is an immutable historical production release. Automatic verification and publication are retired in v4.2.0.'
