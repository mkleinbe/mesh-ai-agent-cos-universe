name: Temporary CFO v4.6.0 Builder Projection Alignment

on:
  push:
    branches: [remediation/cfo-zero-defect-v4.6.0]
    paths:
      - 'scripts/_temporary_cfo_v460_builder_alignment.py'
      - '.github/workflows/_temporary-cfo-v460-builder-alignment.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  align:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Apply Builder projection alignment
        run: |
          set -euo pipefail
          python -m py_compile scripts/_temporary_cfo_v460_builder_alignment.py
          python scripts/_temporary_cfo_v460_builder_alignment.py
          rm scripts/_temporary_cfo_v460_builder_alignment.py
          git diff --check
      - name: Commit aligned projection
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add chatgpt/workspace-agents/cfo.json tests/evaluations/test_cfo_execution_v460.py scripts/_temporary_cfo_v460_builder_alignment.py
          git commit -m 'fix(cfo): align Builder source-scope projection'
          git push origin HEAD:remediation/cfo-zero-defect-v4.6.0
