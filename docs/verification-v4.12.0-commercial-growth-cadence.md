# Verification v4.12.0: Commercial Growth Cadence

Independent verification requires the exact candidate to prove:

- full repository CI and 100 percent branch-aware `mesh_cos` coverage remain green;
- the 10-agent registry is unchanged;
- `LOOP-COM-001` is the single scheduled Commercial Growth dispatcher;
- scheduler wake does not equal business progress;
- monthly review due-state behavior;
- quarterly review collision coalescing;
- stable idempotency across retries;
- ad hoc, scheduled, and native-event invocation semantics;
- polling is explicitly rejected as native-event compliance;
- `LOOP-COM-HITL-001` authority remains intact;
- fresh evidence reuse is explicit;
- four commercial business states remain deterministic;
- completion remains distinct from verification;
- Revenue Intelligence and GTM Orchestrator authority boundaries remain unchanged;
- v4.11.1 publisher is historical and cannot react to the new main commit;
- the v4.12.0 Skill bundle is deterministic and checksum-valid;
- tag and GitHub Release resolve to the exact final main SHA.

Implementation completion is not verification.
