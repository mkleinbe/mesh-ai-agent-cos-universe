name: v4.13.2 Historical Verification

# Historical publisher retired after v4.13.2 publication.
# v4.13.3 owns current SemVer publication and QNAP 4.4.2 packaging.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-historical:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: test -f docs/release-v4.13.2-qnap-source-identity.md
